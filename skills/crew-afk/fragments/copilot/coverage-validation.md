Pass that prompt to `task(agent_type="general-purpose", prompt=...)`. Do **not** use a cheap agent
type or model tier: coverage validation does genuine reasoning (matching PRD requirements against
merged code and issue acceptance criteria).
